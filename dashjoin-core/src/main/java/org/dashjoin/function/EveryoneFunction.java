package org.dashjoin.function;

/**
 * marker interface for functions that simply transform data and therefore do not need to be
 * protected, since the user had the proper credentials to get the data in the first place
 */
public interface EveryoneFunction {

}
