{
	"ID": "search",
	"query": "select 'EMP', ID, NAME from EMP where NAME=${search} union select 'PRJ', ID, NAME from PRJ where NAME=${search}",
	"type": "read",
	"roles": ["user"],
  "arguments" : {
    "search" : {
      "type" : "string"
    }
  }
}
